Copy foster-syntax/ to ~/.vscode/extensions/
or make a symlink:
    ln -s ~/foster/tools/vscode/foster-syntax ~/.vscode/extensions/

The usersettings.js file has some custom colorizations for specific Foster
constructs.

The Foster.tmLanguage file is derived from the Haskell.tmLanguage file.
