// IN: 2 3 5 8 13

run-clo = fn (f:fn()) {  f()  }

main = fn () {
  let n:i32 = read_i32() in {
  let c:  fn() =   fn () { n + read_i32() }   in {
    expect_i32(5)
    print_i32(run-clo(c))
  }
  }
}
