run-clo = fn (f:fn( to i32) to i32) {  f()  }

main = fn (to i32) {
  let n:i32 = opaquely_i32(2) in {
  let c:  fn(to i32) =   fn (to i32) { n + opaquely_i32(3) }   in {
    expect_i32(5)
    print_i32(run-clo(c))
  }
  }
}
