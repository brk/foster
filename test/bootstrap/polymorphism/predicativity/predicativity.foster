
main = {
  //test1 !;
  //test2 !;
  //test3 !;

  ()
};

/*
test1 = {
  let iid = { x:Int32 => x };
      sfi = { forall a:Boxed, x:{ Int32 => Int32 } => y:a => x };
      //sfi = { x:{ Int32 => Int32 } => y:Int32 => x };
      fii = sfi iid iid;
      // r   = (prim ref fst);
  in
    expect_i32 5; print_i32 (fii 5);
    //expect_i1 false; print_i1 (prim __COMPILES__ (fst         0 0));
    //expect_i1 false; print_i1 (prim __COMPILES__ (fst         0 0));
    //expect_i1 false; print_i1 (prim __COMPILES__ (fst:[Int32] 0 0));
  end
};
*/

/*
test2 = {
  let fst = { forall a:Boxed, x:a => y:a => x };
      iid = { x:Int32 => x };
      fii = fst iid iid;
  in
    0;
  end
};
*/

/*
test3 = {
  let fst = { forall a:Boxed, x:a => y:a => x };
      fff = fst fst:[] fst:[];
      //fff = fst fst fst;
      //ffi = fff:[{Int32 => Int32}];

      //fff = fst:[?a] fst fst;
      //fii = fff iid iid;
  in
    0;
  end
};
*/
