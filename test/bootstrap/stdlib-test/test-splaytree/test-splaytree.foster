snafuinclude Splaytree "splaytree";

main = {
  s = (SplayObj { () } (SplayNil !) (SplayNil !));
  x = splay s { z => EQ ! };
  soca s;
  ()
};

soca :: { SplayTree { () } => () };
soca = { x => () };
