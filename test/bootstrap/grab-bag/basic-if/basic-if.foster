main = {
  iftru ();
  iffls ();
};

iftru = {
  expect_i32 3;
  print_i32  if primitive_==_i32 2 (opaquely_i32 2) then 3 else 4 end;
};

iffls = {
  expect_i32 4;
  print_i32  if primitive_==_i32 1 (opaquely_i32 2) then 3 else 4 end;
};
