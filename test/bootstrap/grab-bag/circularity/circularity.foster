main = { 0 };

weird1 = { forall t:Type,
                  a : Array t =>
                  f1 : { t => () } =>
  prim_arrayLength a;
  f1 a[0];
  weird1 a f1;
  ()
};


weird2 = { forall t:Type,
                  a : Array t =>
                  f2 : { t => () } =>
  prim_arrayLength a;
  f2 a[0];
  ()
};