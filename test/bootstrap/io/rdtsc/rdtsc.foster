main = {
  let s = llvm_readcyclecounter (); in
    // Do some IO to force the two cycle counter readings apart.
    expect_i32 2;
    print_i32 (opaquely_i32 2);
    let e = llvm_readcyclecounter (); in
      expect_i1 true;
      print_i1 ((primitive_sext_i64_i32 128) <Int64 (e -Int64 s));
    end
  end
};
