// IN: 2
// The testing infrastructure looks for lines like the one above and
// feeds the data to stdin

main = fn ( to i32) {
  let s:i64 = llvm_readcyclecounter() in {
    expect_i32(2)
    print_i32(read_i32())
    let e:i64 = llvm_readcyclecounter() in {
      expect_i1(true)
      print_i1( primitive_<_i64(128, primitive_-_i64(e, s)) )
    }
  }
  0
}
