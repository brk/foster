main = {
  expect_i32 3;
  print_i32 (foo 2);
};

foo = { x:i32 =>
  if primitive_==_i32 x 0 then
    0
  else
    let y = primitive_+_i32 x 1;
        z = foo (primitive_-_i32 x 1);
     in y end
  end
};
