main = fn (to i32) {
  expect_i32(3)
  print_i32(foo(2))
}

foo = fn(x:i32 to i32) {
  if (x == 0) then {
    0
  } else {
    let y : i32 = x + 1 in {
      let z : i32 = foo(x - 1) in {
        y
      }
    }
  }
}
